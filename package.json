{
  "name": "comsmanagement",
  "version": "1.0.0",
  "scripts": {
    "client": "cd reactapp/client && yarn start",
    "server": "cd reactapp/ && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
